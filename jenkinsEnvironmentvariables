pipeline {
    agent any
     environment {
        DISABLE_AUTH = 'true'
        DB_ENGINE    = 'sqlite'
    }
    stages { 
        stage('Environment variables') {
            
            steps {
			
            	echo "Updating Configs"
                echo "Database engine is ${DB_ENGINE}"
                
            }
        }

    }
}
