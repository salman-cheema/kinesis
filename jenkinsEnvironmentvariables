pipeline {
    agent any
     environment {
        KINESIS_STATUS_DATA_STREAM = "${env.KINESIS_DATA_STREAM_STATUS}"
    }
    stages { 
        stage('Environment variables') {
            
            steps {
			
            	echo "Updating Configs"
                echo "Database engine is ${KINESIS_STATUS_DATA_STREAM}"
                
            }
        }

    }
}
