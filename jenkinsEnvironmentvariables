pipeline {
    agent any
    stages { 
        stage('Environment variables') {
            
            steps {
			
            	echo "Updating Configs"
                echo "Database engine is ${env.KINESIS_STATUS_DATA_STREAM}"
                
            }
        }

    }
}
